[flake8]
# Baseline gate for the first CI ruleset. The current codebase does not
# follow the full ament style profile yet, so start by blocking syntax errors
# and undefined-name style issues while leaving format cleanup for a separate
# pass.
select = E9,F63,F7,F82
max-line-length = 120
