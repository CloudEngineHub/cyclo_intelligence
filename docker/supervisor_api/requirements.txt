# supervisor_api — FastAPI control plane (PLAN §4.7 + §4.8).
#
# 'docker' (docker-py) drives /backends/{name}/{pull,status,start,stop}
# through the Engine API on /var/run/docker.sock, including creating
# policy containers when they are not present yet.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
docker>=6.0,<8.0
