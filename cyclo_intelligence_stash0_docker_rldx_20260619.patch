diff --git a/docker/Dockerfile.arm64 b/docker/Dockerfile.arm64
index f8ab295..a469618 100644
--- a/docker/Dockerfile.arm64
+++ b/docker/Dockerfile.arm64
@@ -8,7 +8,7 @@
 #                                         orchestrator / cyclo_data nodes
 #
 # Build context: cyclo_intelligence/ (repo root)
-#   docker build -f docker/Dockerfile.arm64 -t robotis/cyclo-intelligence:0.1.13-arm64 .
+#   docker build -f docker/Dockerfile.arm64 -t robotis/cyclo-intelligence:0.1.15-arm64 .
 #
 # lerobot / groot are separate containers (PLAN §4.8 on-demand pull);
 # this image intentionally does NOT bundle PyTorch / CUDA / LeRobot.
diff --git a/docker/container.sh b/docker/container.sh
index d1e364b..17c0f0e 100755
--- a/docker/container.sh
+++ b/docker/container.sh
@@ -4,7 +4,7 @@
 # and manages the main runtime plus optional policy containers.
 #
 # Usage:
-#   docker/container.sh start              # → cyclo_intelligence
+#   docker/container.sh start              # → cyclo_intelligence + rldx runtime
 #   docker/container.sh start-lerobot      # → lerobot (idle until LOAD)
 #   docker/container.sh start-groot        # → groot (idle until LOAD)
 #   docker/container.sh start-rldx         # → rldx runtime/client (idle until LOAD)
@@ -72,7 +72,29 @@ set -- "${NEW_ARGS[@]}"
 # host without sudo). In auto mode, prefer SSD-backed storage when it is
 # usable and fall back to repo-local storage on development machines.
 ensure_host_dir() {
-    [ -d "$1" ] || mkdir -p "$1"
+    local dir="$1"
+    local uid gid
+
+    uid="$(id -u)"
+    gid="$(id -g)"
+
+    if [ ! -d "$dir" ]; then
+        if ! mkdir -p "$dir" 2>/dev/null; then
+            sudo install -d -o "$uid" -g "$gid" "$dir" || true
+        fi
+    fi
+
+    if [ ! -w "$dir" ]; then
+        echo "[container.sh] Fixing host bind-mount directory permissions: $dir"
+        sudo chown "$uid:$gid" "$dir" 2>/dev/null || true
+        sudo chmod u+rwx "$dir" 2>/dev/null || true
+    fi
+
+    if [ ! -d "$dir" ] || [ ! -w "$dir" ]; then
+        echo "[container.sh] Error: host bind-mount directory is not writable: $dir" >&2
+        echo "[container.sh] Run: sudo install -d -o $uid -g $gid '$dir'" >&2
+        exit 1
+    fi
 }
 
 storage_root_usable() {
@@ -275,7 +297,7 @@ show_help() {
 Usage: $0 <command>
 
 Main image (cyclo_intelligence):
-  start            Build (if needed) and start cyclo_intelligence
+  start            Build (if needed) and start cyclo_intelligence + rldx
   enter            Open an interactive bash in cyclo_intelligence
   logs             Tail cyclo_intelligence logs
 
@@ -323,7 +345,8 @@ Flags (any start* command):
 
 Environment:
   GPU_ARCH         default | blackwell   (optional, amd64 only)
-  VERSION          image tag version (default: 0.1.13 for cyclo)
+  VERSION          cyclo image tag version (default: 0.1.15)
+  RLDX_VERSION     rldx client image tag version (default: 0.1.1)
   ROS_DOMAIN_ID    default 30
   CYCLO_STORAGE_MODE
                    auto | ssd | local (default auto). Auto uses
@@ -343,16 +366,22 @@ EOF
 }
 
 ui_dir() {
-    canonical_path "${SCRIPT_DIR}/../orchestrator/ui"
+    canonical_path "${CYCLO_UI_DIR:-${SCRIPT_DIR}/../orchestrator/ui}"
 }
 
 main_ui_dir() {
     printf '%s\n' "/root/ros2_ws/src/cyclo_intelligence/orchestrator/ui"
 }
 
-main_container_has_npm() {
-    container_running "$MAIN_CONTAINER" \
-        && docker exec "$MAIN_CONTAINER" sh -lc 'command -v npm >/dev/null 2>&1'
+main_container_can_run_ui_npm() {
+    container_running "$MAIN_CONTAINER" || return 1
+    docker exec \
+        -u "$(id -u):$(id -g)" \
+        -e HOME=/tmp \
+        -w "$(main_ui_dir)" \
+        "$MAIN_CONTAINER" \
+        sh -lc 'command -v npm >/dev/null 2>&1 && test -w . && node -e "require(\"fs\").readFileSync(process.cwd()+\"/package.json\")"' \
+        >/dev/null 2>&1
 }
 
 run_ui_npm_in_main() {
@@ -377,17 +406,53 @@ run_ui_npm_external() {
 }
 
 run_ui_npm() {
-    if main_container_has_npm; then
+    if main_container_can_run_ui_npm; then
         run_ui_npm_in_main "$@"
     else
         run_ui_npm_external "$@"
     fi
 }
 
+ensure_ui_generated_dirs_writable() {
+    local dir uid gid
+    local -a targets
+    dir="$(ui_dir)"
+    uid="$(id -u)"
+    gid="$(id -g)"
+    targets=()
+
+    if [ -e "${dir}/node_modules" ] \
+        && { [ ! -w "${dir}/node_modules" ] \
+            || { [ -e "${dir}/node_modules/.cache" ] && [ ! -w "${dir}/node_modules/.cache" ]; }; }; then
+        targets+=("node_modules")
+    fi
+    if [ -e "${dir}/build" ] && [ ! -w "${dir}/build" ]; then
+        targets+=("build")
+    fi
+
+    if [ "${#targets[@]}" -eq 0 ]; then
+        return 0
+    fi
+
+    echo "[container.sh] Fixing UI generated directory permissions: ${targets[*]}"
+    docker run --rm --network none \
+        -v "${dir}:/ui" \
+        -w /ui \
+        "${CYCLO_UI_NODE_IMAGE:-node:22}" \
+        sh -c "chown -R ${uid}:${gid} ${targets[*]} && chmod -R u+rwX ${targets[*]}"
+}
+
 ensure_ui_dependencies() {
     local dir
-    if main_container_has_npm; then
-        if docker exec "$MAIN_CONTAINER" test -x "$(main_ui_dir)/node_modules/.bin/react-scripts"; then
+    ensure_ui_generated_dirs_writable
+
+    if main_container_can_run_ui_npm; then
+        if docker exec \
+            -u "$(id -u):$(id -g)" \
+            -e HOME=/tmp \
+            -w "$(main_ui_dir)" \
+            "$MAIN_CONTAINER" \
+            test -x node_modules/.bin/react-scripts; then
             return 0
         fi
 
@@ -435,8 +500,10 @@ build_ui() {
         return 0
     fi
 
-    echo "[container.sh] Copying UI build into ${MAIN_CONTAINER} nginx root..."
+    echo "[container.sh] Copying UI build and nginx config into ${MAIN_CONTAINER}..."
     docker cp "${dir}/build/." "${MAIN_CONTAINER}:/usr/share/nginx/html/"
+    docker cp "${dir}/nginx.conf" "${MAIN_CONTAINER}:/etc/nginx/conf.d/default.conf"
+    docker exec "$MAIN_CONTAINER" nginx -t
     docker exec "$MAIN_CONTAINER" sh -c 'nginx -s reload 2>/dev/null || true'
     echo "[container.sh] UI updated. Refresh the browser to load the new bundle."
 }
@@ -455,11 +522,14 @@ start_main() {
     setup_storage
     setup_x11
     echo "[container.sh] Pulling pre-built images (ignoring local-only failures)..."
-    $COMPOSE pull --ignore-pull-failures "$MAIN_SERVICE" "$LEROBOT_SERVICE" "$GROOT_SERVICE" || true
+    $COMPOSE pull --ignore-pull-failures "$MAIN_SERVICE" "$LEROBOT_SERVICE" "$GROOT_SERVICE" "$RLDX_SERVICE" || true
     remove_legacy_rldx_container
     remove_stale_policy_containers
-    echo "[container.sh] Starting $MAIN_SERVICE (ARCH=$ARCH${BUILD_FLAG:+, rebuild on})..."
-    $COMPOSE up -d $BUILD_FLAG "$MAIN_SERVICE"
+    echo "[container.sh] Starting $MAIN_SERVICE + $RLDX_SERVICE (ARCH=$ARCH${BUILD_FLAG:+, rebuild on})..."
+    echo "[container.sh]   RLDX_ZMQ_HOST=${RLDX_ZMQ_HOST:-127.0.0.1}"
+    echo "[container.sh]   RLDX_ZMQ_PORT=${RLDX_ZMQ_PORT:-5555}"
+    echo "[container.sh]   RLDX_ZMQ_TIMEOUT_MS=${RLDX_ZMQ_TIMEOUT_MS:-300000}"
+    $COMPOSE up -d $BUILD_FLAG "$MAIN_SERVICE" "$RLDX_SERVICE"
     echo "[container.sh] Done. 'docker/container.sh status' to check s6 services."
 }
 
diff --git a/docker/docker-compose.yml b/docker/docker-compose.yml
index 4e9d6e7..efa9db3 100644
--- a/docker/docker-compose.yml
+++ b/docker/docker-compose.yml
@@ -11,7 +11,7 @@
 #
 # Usage: prefer the container.sh wrapper — it auto-detects ARCH from
 # `uname -m` and sets the compose env:
-#   docker/container.sh start              # → cyclo_intelligence only
+#   docker/container.sh start              # → cyclo_intelligence + rldx runtime
 #   docker/container.sh start-lerobot      # → lerobot (no ROBOT_TYPE
 #                                          #    needed; passed at LOAD)
 #   docker/container.sh enter              # → bash inside cyclo_intelligence
@@ -43,7 +43,7 @@ services:
   # ============================================================
   cyclo_intelligence:
     container_name: cyclo_intelligence
-    image: robotis/cyclo-intelligence:${VERSION:-0.1.13}-${ARCH:-arm64}
+    image: robotis/cyclo-intelligence:${VERSION:-0.1.15}-${ARCH:-arm64}
     build:
       context: ..
       dockerfile: docker/Dockerfile.${ARCH:-arm64}
@@ -76,6 +76,7 @@ services:
       - ZENOH_ROUTER_PORT=7447
       - ZENOH_CONFIG_OVERRIDE=transport/shared_memory/enabled=true
       - CYCLO_UI_PORT=${CYCLO_UI_PORT:-80}
+      - RLDX_VERSION=${RLDX_VERSION:-0.1.1}
       # LOAD may include first-time gated backbone downloads (e.g. Cosmos).
       # Keep this long enough that a real download is not surfaced as a
       # false model-load failure while the container continues downloading.
@@ -381,7 +382,7 @@ services:
   # ============================================================
   rldx:
     container_name: rldx_runtime
-    image: robotis/rldx-zenoh:0.1.0-${ARCH:-arm64}
+    image: robotis/rldx-zenoh:${RLDX_VERSION:-0.1.1}-${ARCH:-arm64}
     build:
       context: ../cyclo_brain/policy
       dockerfile: rldx/Dockerfile.${ARCH:-arm64}
diff --git a/docker/supervisor_api/app.py b/docker/supervisor_api/app.py
index 3ae03c2..176ab7c 100644
--- a/docker/supervisor_api/app.py
+++ b/docker/supervisor_api/app.py
@@ -185,6 +185,7 @@ def _detect_arch() -> str:
 
 
 _BACKEND_ARCH = os.environ.get("ARCH", _detect_arch())
+_RLDX_VERSION = os.environ.get("RLDX_VERSION", "0.1.1")
 
 
 # Image versions are hardcoded per backend below since each service has
@@ -207,7 +208,7 @@ _BACKENDS: Dict[str, Dict[str, str]] = {
     "rldx": {
         "service": "rldx",
         "container": "rldx_runtime",
-        "image": f"robotis/rldx-zenoh:0.1.0-{_BACKEND_ARCH}",
+        "image": f"robotis/rldx-zenoh:{_RLDX_VERSION}-{_BACKEND_ARCH}",
         "services": ["main-runtime", "engine-process"],
     },
 }
diff --git a/docker/test_container_sh.py b/docker/test_container_sh.py
index 8b3c1b7..d4dd410 100644
--- a/docker/test_container_sh.py
+++ b/docker/test_container_sh.py
@@ -14,7 +14,7 @@ def test_start_pulls_main_and_policy_images(tmp_path):
         "printf '%s\\n' \"$*\" >> \"$DOCKER_STUB_LOG\"\n"
         "case \" $* \" in\n"
         "  *' config --format json '*)\n"
-        "    printf '%s\\n' '{\"services\":{\"lerobot\":{\"image\":\"robotis/lerobot-zenoh:1.3.0-arm64\"},\"groot\":{\"image\":\"robotis/groot-zenoh:1.3.0-arm64\"}}}'\n"
+        "    printf '%s\\n' '{\"services\":{\"lerobot\":{\"image\":\"robotis/lerobot-zenoh:1.3.0-arm64\"},\"groot\":{\"image\":\"robotis/groot-zenoh:1.3.0-arm64\"},\"rldx\":{\"image\":\"robotis/rldx-zenoh:0.1.1-arm64\"}}}'\n"
         "    ;;\n"
         "esac\n"
         "if [ \"$1\" = image ] && [ \"$2\" = inspect ]; then\n"
@@ -45,7 +45,11 @@ def test_start_pulls_main_and_policy_images(tmp_path):
 
     docker_calls = log_path.read_text().splitlines()
     assert any(
-        "pull --ignore-pull-failures cyclo_intelligence lerobot groot" in call
+        "pull --ignore-pull-failures cyclo_intelligence lerobot groot rldx" in call
+        for call in docker_calls
+    )
+    assert any(
+        "up -d cyclo_intelligence rldx" in call
         for call in docker_calls
     )
 
@@ -58,7 +62,7 @@ def test_start_removes_policy_container_with_stale_workspace_mount(tmp_path):
         "printf '%s\\n' \"$*\" >> \"$DOCKER_STUB_LOG\"\n"
         "case \" $* \" in\n"
         "  *' config --format json '*)\n"
-        "    printf '%s\\n' '{\"services\":{\"lerobot\":{\"image\":\"robotis/lerobot-zenoh:1.3.0-arm64\"},\"groot\":{\"image\":\"robotis/groot-zenoh:1.3.0-arm64\"}}}'\n"
+        "    printf '%s\\n' '{\"services\":{\"lerobot\":{\"image\":\"robotis/lerobot-zenoh:1.3.0-arm64\"},\"groot\":{\"image\":\"robotis/groot-zenoh:1.3.0-arm64\"},\"rldx\":{\"image\":\"robotis/rldx-zenoh:0.1.1-arm64\"}}}'\n"
         "    exit 0\n"
         "    ;;\n"
         "esac\n"
@@ -168,3 +172,139 @@ def test_start_lerobot_removes_stale_workspace_mount(tmp_path):
         call == "rm -f lerobot_server"
         for call in docker_calls
     )
+
+
+def test_start_rldx_repairs_unwritable_model_parent(tmp_path):
+    docker_log_path = tmp_path / "docker.log"
+    sudo_log_path = tmp_path / "sudo.log"
+    docker_stub = tmp_path / "docker"
+    docker_stub.write_text(
+        "#!/bin/sh\n"
+        "printf '%s\\n' \"$*\" >> \"$DOCKER_STUB_LOG\"\n"
+        "case \" $* \" in\n"
+        "  *' config --format json '*)\n"
+        "    printf '%s\\n' '{\"services\":{\"rldx\":{\"image\":\"robotis/rldx-zenoh:0.1.1-arm64\"}}}'\n"
+        "    exit 0\n"
+        "    ;;\n"
+        "esac\n"
+        "if [ \"$1\" = image ] && [ \"$2\" = inspect ]; then\n"
+        "  printf '%s\\n' 'sha256:current'\n"
+        "  exit 0\n"
+        "fi\n"
+        "exit 0\n"
+    )
+    docker_stub.chmod(0o755)
+
+    sudo_stub = tmp_path / "sudo"
+    sudo_stub.write_text(
+        "#!/bin/sh\n"
+        "printf '%s\\n' \"$*\" >> \"$SUDO_STUB_LOG\"\n"
+        "exec \"$@\"\n"
+    )
+    sudo_stub.chmod(0o755)
+
+    workspace = tmp_path / "workspace"
+    model_dir = workspace / "model"
+    model_dir.mkdir(parents=True)
+    model_dir.chmod(0o555)
+
+    env = {
+        **os.environ,
+        "PATH": f"{tmp_path}:{os.environ['PATH']}",
+        "DOCKER_STUB_LOG": str(docker_log_path),
+        "SUDO_STUB_LOG": str(sudo_log_path),
+        "CYCLO_AGENT_SOCKETS_DIR": str(tmp_path / "agent_sockets"),
+        "CYCLO_STORAGE_MODE": "local",
+        "CYCLO_LOCAL_WORKSPACE_DIR": str(workspace),
+        "CYCLO_LOCAL_HUGGINGFACE_DIR": str(tmp_path / "huggingface"),
+    }
+
+    try:
+        subprocess.run(
+            [str(REPO_ROOT / "docker" / "container.sh"), "start-rldx"],
+            cwd=REPO_ROOT,
+            env=env,
+            check=True,
+            text=True,
+            capture_output=True,
+        )
+    finally:
+        model_dir.chmod(0o755)
+
+    assert (model_dir / "lerobot").is_dir()
+    assert (model_dir / "groot").is_dir()
+    assert (model_dir / "rldx").is_dir()
+    assert os.access(model_dir / "rldx", os.W_OK)
+    assert any(
+        call.startswith("chmod u+rwx ") and str(model_dir) in call
+        for call in sudo_log_path.read_text().splitlines()
+    )
+
+
+def test_build_ui_falls_back_when_main_ui_dir_is_not_user_accessible(tmp_path):
+    log_path = tmp_path / "docker.log"
+    ui_dir = tmp_path / "ui"
+    ui_cache = ui_dir / "node_modules" / ".cache"
+    ui_cache.mkdir(parents=True)
+    (ui_dir / "package.json").write_text("{}\n")
+    ui_cache.chmod(0o555)
+
+    docker_stub = tmp_path / "docker"
+    docker_stub.write_text(
+        "#!/bin/sh\n"
+        "printf '%s\\n' \"$*\" >> \"$DOCKER_STUB_LOG\"\n"
+        "if [ \"$1\" = ps ]; then\n"
+        "  printf '%s\\n' 'cyclo_intelligence'\n"
+        "  exit 0\n"
+        "fi\n"
+        "if [ \"$1\" = exec ] && [ \"$2\" = -u ]; then\n"
+        "  exit 13\n"
+        "fi\n"
+        "exit 0\n"
+    )
+    docker_stub.chmod(0o755)
+
+    env = {
+        **os.environ,
+        "PATH": f"{tmp_path}:{os.environ['PATH']}",
+        "DOCKER_STUB_LOG": str(log_path),
+        "CYCLO_UI_DIR": str(ui_dir),
+    }
+
+    try:
+        subprocess.run(
+            [str(REPO_ROOT / "docker" / "container.sh"), "build-ui"],
+            cwd=REPO_ROOT,
+            env=env,
+            check=True,
+            text=True,
+            capture_output=True,
+        )
+    finally:
+        ui_cache.chmod(0o755)
+
+    docker_calls = log_path.read_text().splitlines()
+    assert any(
+        call.startswith("run --rm --network none ") and " chown -R " in call and "node_modules" in call
+        for call in docker_calls
+    )
+    assert any(
+        call.startswith("run --rm --network host ") and " npm run build" in call
+        for call in docker_calls
+    )
+    assert not any(
+        call.startswith("exec -u ") and " npm run build" in call
+        for call in docker_calls
+    )
+    assert any(
+        call.startswith("cp ") and " cyclo_intelligence:/usr/share/nginx/html/" in call
+        for call in docker_calls
+    )
+    assert any(
+        call.startswith("cp ") and "nginx.conf cyclo_intelligence:/etc/nginx/conf.d/default.conf" in call
+        for call in docker_calls
+    )
+    assert any(
+        call == "exec cyclo_intelligence nginx -t"
+        for call in docker_calls
+    )
diff --git a/shared/shared/robot_configs/ffw_sg2_rev2_config.yaml b/shared/shared/robot_configs/ffw_sg2_rev2_config.yaml
new file mode 100644
index 0000000..90038a3
--- /dev/null
+++ b/shared/shared/robot_configs/ffw_sg2_rev2_config.yaml
@@ -0,0 +1,140 @@
+orchestrator:
+  ros__parameters:
+    ffw_sg2_rev2:
+      robot_name: "FFW SG2 Rev2"
+      urdf_path: urdf/ffw_sg2_follower.urdf
+
+      # Observation: every signal the policy reads from the robot.
+      observation:
+        images:
+          cam_head_left:
+            topic: /zed/zed_node/left/image_rect_color/compressed
+            msg_type: sensor_msgs/msg/CompressedImage
+          cam_head_right:
+            topic: /zed/zed_node/right/image_rect_color/compressed
+            msg_type: sensor_msgs/msg/CompressedImage
+          cam_wrist_left:
+            topic: /camera_left/camera_left/color/image_rect_raw/compressed
+            msg_type: sensor_msgs/msg/CompressedImage
+            rotation_deg: 270
+          cam_wrist_right:
+            topic: /camera_right/camera_right/color/image_rect_raw/compressed
+            msg_type: sensor_msgs/msg/CompressedImage
+            rotation_deg: 270
+
+        state:
+          # Each entry contributes one slice of observation.state.
+          # Order is YAML insertion order.
+          follower_left_arm:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - arm_l_joint1
+              - arm_l_joint2
+              - arm_l_joint3
+              - arm_l_joint4
+              - arm_l_joint5
+              - arm_l_joint6
+              - arm_l_joint7
+          follower_left_hand:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - gripper_l_joint1
+          follower_right_arm:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - arm_r_joint1
+              - arm_r_joint2
+              - arm_r_joint3
+              - arm_r_joint4
+              - arm_r_joint5
+              - arm_r_joint6
+              - arm_r_joint7
+          follower_right_hand:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - gripper_r_joint1
+          follower_head:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - head_joint1
+              - head_joint2
+          follower_lift:
+            topic: /joint_states
+            msg_type: sensor_msgs/msg/JointState
+            joint_names:
+              - lift_joint
+          # mobile:
+          #   topic: /odom
+          #   msg_type: nav_msgs/msg/Odometry
+          #   joint_names:
+          #     - linear_x
+          #     - linear_y
+          #     - angular_z
+
+      # Action: every modality the policy commands.
+      action:
+        left_arm:
+          topic: /leader/joint_trajectory_command_broadcaster_left/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - arm_l_joint1
+            - arm_l_joint2
+            - arm_l_joint3
+            - arm_l_joint4
+            - arm_l_joint5
+            - arm_l_joint6
+            - arm_l_joint7
+        left_hand:
+          topic: /leader/joint_trajectory_command_broadcaster_left/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - gripper_l_joint1
+        right_arm:
+          topic: /leader/joint_trajectory_command_broadcaster_right/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - arm_r_joint1
+            - arm_r_joint2
+            - arm_r_joint3
+            - arm_r_joint4
+            - arm_r_joint5
+            - arm_r_joint6
+            - arm_r_joint7
+        right_hand:
+          topic: /leader/joint_trajectory_command_broadcaster_right/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - gripper_r_joint1
+        head:
+          topic: /leader/joystick_controller_left/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - head_joint1
+            - head_joint2
+        lift:
+          topic: /leader/joystick_controller_right/joint_trajectory
+          msg_type: trajectory_msgs/msg/JointTrajectory
+          joint_names:
+            - lift_joint
+        # mobile:
+        #   topic: /cmd_vel
+        #   msg_type: geometry_msgs/msg/Twist
+        #   joint_names:
+        #     - linear_x
+        #     - linear_y
+        #     - angular_z
+
+      # Recording: auxiliary topics that need to be in the bag but are
+      # not VLA training data.
+      recording:
+        extra_topics:
+          - /tf
+          - /zed/zed_node/left/camera_info
+          - /zed/zed_node/right/camera_info
+          - /camera_left/camera_left/color/camera_info
+          - /camera_right/camera_right/color/camera_info
